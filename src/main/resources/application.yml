spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jwt_Security
    username: postgres
    password: l8n3_a7-c2.E
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect